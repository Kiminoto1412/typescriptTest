import { merge } from "./mergeIntArray";

// Example usage
const collection1 = [ 3, 5, 1];
const collection2 = [2, 4, 6];

const mergedCollection = merge(collection1, collection2);
console.log("Merged Collection:", mergedCollection);
